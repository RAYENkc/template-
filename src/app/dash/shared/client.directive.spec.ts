import { ClientDirective } from './client.directive';

describe('ClientDirective', () => {
  it('should create an instance', () => {
    const directive = new ClientDirective();
    expect(directive).toBeTruthy();
  });
});
